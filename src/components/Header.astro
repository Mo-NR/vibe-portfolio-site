---
interface Props {
  currentPage?: string;
}

const { currentPage = '' } = Astro.props;
---

<header class="border-b border-green-dark">
  <nav class="max-w-[960px] mx-auto px-6 py-6 flex justify-between items-center">
    <a href="/" class="text-2xl font-heading font-bold uppercase tracking-tight">
      Mohammed Rahman
    </a>
    <ul class="flex gap-8">
      <li>
        <a 
          href="/" 
          class={`font-body text-sm uppercase tracking-wide transition-colors ${
            currentPage === 'home' ? 'text-green-light' : 'text-white/80 hover:text-green-light'
          }`}
        >
          Home
        </a>
      </li>
      <li>
        <a 
          href="/about" 
          class={`font-body text-sm uppercase tracking-wide transition-colors ${
            currentPage === 'about' ? 'text-green-light' : 'text-white/80 hover:text-green-light'
          }`}
        >
          About
        </a>
      </li>
      <li>
        <a 
          href="/projects" 
          class={`font-body text-sm uppercase tracking-wide transition-colors ${
            currentPage === 'projects' ? 'text-green-light' : 'text-white/80 hover:text-green-light'
          }`}
        >
          Projects
        </a>
      </li>
    </ul>
  </nav>
</header>

